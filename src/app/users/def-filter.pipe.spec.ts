import { DefFilterPipe } from './def-filter.pipe';

describe('DefFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DefFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
